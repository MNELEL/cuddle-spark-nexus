עדכון MERGE_MEMORY.md — תיעוד שני מקרי הבדיקה החדשים בטסט זרימת class_notifications

## מטרה
לעדכן את `MERGE_MEMORY.md` כך שיסכם גם את שני מקרי הבדיקה החדשים שהתווספו לקובץ `src/test/notifications-flow.test.ts`:
1. סינון recipient לא תואם (recipient filter mismatch) — כולל recipient של משתמש אחר, UUID זר, ושאילתה ללא פילטר כלל.
2. משתמש עם תפקיד `institution_admin` (principal) — וידוא שגם מנהל מוסד של הכיתה רואה רק את ההתראות של עצמו ולא יכול לסמן/לקרוא התראות של מורה אחר.

## שינויים מתוכננים

### 1. עדכון תאריך ותקציר שינויים
- בעדכון אחרון: 9 באוגוסט 2026 — נוספו שני מקרי בדיקה לטסט זרימת `class_notifications` (recipient mismatch ו-`institution_admin`).
- היסטוריית עדכונים: שורה קצרה ל-9/8 עם תיאור השינוי.

### 2. עדכון סעיף 12.4 — תיאור הטסט המעודכן
בטקסט הקיים:
> "טסט זרימה מקצה-לקצה שמשחזר בדיוק את השאילתות של `listUnreadClassNotifications` / `markNotificationRead`..."

להוסיף בהמשך:
- שני מקרי בדיקה נוספים לבדיקת חוסם recipient: חוסם לא תואם ומנהל מוסד (principal).
- ציון שכל הטסטים עוברים (מספר ה-`it()` בקובץ נשאר 6; סה"כ כללי 91 מקרי בדיקה).

### 3. עדכון טבלת 12.4.1
בשורה "RLS מידע רגיש והתראות":
- `notifications-flow.test.ts` — לעדכן את התיאור: 6 מקרי בדיקה, כולל שני מקרי recipient חדשים.
- סה"כ קבצים וסה"כ `it()` נשארים 16 / 91 (אם נכון), או יתעדכנו בהתאם למספר בפועל.

### 4. עדכון סעיף 12.4.2 — קישורים ישירים לקובץ ולמקרים
- להוסיף קישורים לשורות ספציפיות בקובץ:
  - `src/test/notifications-flow.test.ts` — קובץ הטסט המלא.
  - מקרה "recipient filter mismatch" — קישור לשורות 125-147.
  - מקרה "institution_admin (principal)" — קישור לשורות 149-180.

### 5. ולידציה
- לאחר העריכה, לוודא שהמסמך עדיין קריא ונכון בעריכת Markdown.
- לא משנים קוד, לא מריצים בדיקות — זהו עדכון תיעוד בלבד.
