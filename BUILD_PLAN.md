# BUILD_PLAN.md — ClassAlign Studio / Harmony Hub
**מאחד:** `LOVABLE_BUILD_GUIDE.md` (27/7) + `REMAINING_TASKS.md` (28/7) + הסטטוס הרלוונטי מ-`MERGE_MEMORY.md` (28/7).
**עדכון אחרון:** 28 ביולי 2026.

**עיקרון:** מסמך זה הוא היחיד שצריך מעכשיו. הוא מכיל **רק** מה שעדיין נשאר לבנות בפועל, אחרי אימות קוד-בפועל (`Lovable:read_file`). כל מה שהתברר כבר-קיים מופיע בקצרה בסעיף "כבר קיים" למניעת כפילות, ולא חוזר בשום מקום אחר במסמך.

---

## 🚧 חסם פעיל: workspace Lovable אזל בקרדיטים
לפני הזנת **כל** שכבה למטה ל-Lovable (`send_message`): יש לטעון קרדיטים ב-https://lovable.dev/settings/billing (לא ניתן דרך GitHub — זה חיבור sync בלבד). שתי הודעות ממתינות נדחו בגלל זה (ראה סעיף "הודעות מוכנות לשליחה חוזרת" למטה).

לפני הזנת כל שכבה: לוודא עם `Lovable:read_file` שהמבנה (שמות טבלאות/שדות) עדיין תואם למצב בפועל באותו רגע — זה מה שמנע טעות כפולה במסמכים הקודמים.

---

## ✅ כבר קיים בפועל — לא לגעת שוב

| פריט | מה קיים בפועל |
|---|---|
| הגרלה כיתתית | `_authenticated.raffle.$classId.tsx` — גלגל-מזל מלא (תלמידים/פרסים/ניקוד קבוצתי), מעבר לתכנון המקורי |
| שמירת תצורות ישיבה | `seating-configs.functions.ts` — snapshot מלא |
| נגישות בגריד ישיבה | `seating-grid.tsx` — מצב a11y מלא (מקלדת + קורא מסך + ניגודיות) |
| מנוע סידור בסיסי | `seating-logic.ts` — מנוע דטרמיניסטי (ניקוד אילוצים + random-restart), **בלי AI/reasoning** — זהו ה-fallback הקיים לשכבה 1 למטה |
| Weekly Schedule | `weekly-schedule.functions.ts` — CRUD מלא, מקושר ל-`library_item_id` |
| Student View | `_authenticated.student-view.$classId.tsx` — תצוגת שם-פרטי-בלבד, ללא שדות רגישים |
| תיקון 500 ב-Base44 `registerDriveWatch` | הושלם ב-28/7 — כרטיס UI ב-`AdminDashboard.jsx` קורא דרך `base44.functions.invoke()` במקום `fetch()` גולמי |

---

## 🔴 מה שנשאר לבנות — סדר ביצוע מומלץ

### 1. Fallback דטרמיניסטי ל-`buildWeeklySummary`
**סטטוס:** `ai-weekly-summary.functions.ts` עדיין מחזיר הודעת שגיאה גנרית ב-`catch`, לא סיכום אמיתי. **הכי קטן, הכי מהיר, אין תלות — לבצע ראשון.**

הנחיה ל-Lovable:
> ב-`buildWeeklySummary`, במקום שה-`catch` של `callLovableAI` יחזיר הודעת שגיאה גנרית — הוסף פונקציה `buildDeterministicWeeklySummary(compact)` שבונה סיכום עברי אמיתי מהנתונים ש-`compact` כבר מכיל (אירועים, שיעורים, ניקוד התנהגות, נוכחות), בפורמט Markdown דומה לזה שה-AI מייצר. אין צורך בשאילתות DB נוספות.

---

### 2. תבניות ישיבה מוכנות בקליק
**סטטוס:** נבדק `seating-grid.tsx` במלואו — יש רק "מיון חכם" ו"מיון אקראי", אין שום כפתור תבנית. **קטן, עצמאי, לא תלוי ב-AI — לבצע שני.**

הנחיה ל-Lovable:
> הוסף לפאנל הכלים הקיים ב-`seating-grid.tsx` (לצד "מיון חכם"/"מיון אקראי") 3 כפתורים, כל אחד פועל על `setSeatFn` הקיים (אין צורך ב-endpoint חדש):
> - **שורות קלאסיות** — מילוי רציף שורה-אחר-שורה.
> - **צורת ח׳ (U)** — תלמידים רק בהיקף הגריד (שורה ראשונה + טור ראשון + טור אחרון).
> - **קבוצות עם מעברים** — מילוי רציף עם טורים ריקים קבועים למסדרונות.
>
> כל תבנית מחליפה שיוכים קיימים — עם toast/אישור לפני מחיקת סידור קיים.

---

### 3. אשף הושבה AI עם שקלול + `reasoning` גלוי
**סטטוס:** הפער המרכזי. `seating-logic.ts` הקיים הוא מנוע ניקוד-אילוצים מצוין אך **חסר**: שקלול AI ב-4 סליידרים, שדה הנחיה חופשית, והסבר-בעברית (`reasoning`) למורה. זו הייתה הנקודה שהפכה את הרעיון המקורי (Firebase project) לייחודי. **הכי גדול — לבצע אחרון, אחרי ש-1 ו-2 נכנסו.**

**עקרון מרכזי:** לא לזרוק את `seating-logic.ts` — **להוסיף עליו שכבת AI אופציונלית**, כשה-AI עצמו יכול להשתמש באותו מנוע לבדוק את הצעתו, ובכשל AI ליפול חזרה בדיוק ל-`smartAssign` הקיים (fallback כבר מוכן).

**3.1 מיגרציה — טבלה חדשה: ✅ בוצע בפועל (28/7, דרך Supabase SQL editor — לא דרך Lovable AI). אומת: `information_schema.columns` + `pg_policies` תואמים 100% לתכנון (6 עמודות עם הדיפולטים הנכונים; policy יחיד `FOR ALL` עם `auth.uid() = user_id` ב-USING וב-WITH CHECK). אין צורך לפעול שוב על סעיף זה — לעבור ישר ל-3.2.**

**3.2 קובץ שרת חדש — `src/lib/seating-wizard.functions.ts`:**
> צור `createServerFn` בשם `runSeatingWizard` שמקבל: `classId`, 4 משקלים (academic/behavioral/social/height, 0-100), ו-`freeInstruction` (עד 500 תווים). הפונקציה:
> 1. שולפת תלמידי הכיתה + ציונים אחרונים + `behavior_notes` + שיוכי קבוצה קיימים.
> 2. מחשבת ניתוח-כיתה מקדים: כמה "מתקשים" (ממוצע מתחת 70), כמה "מצטיינים" (מעל 90), כמה עם מילות מפתח לקשיי-קשב ("קשב"/"ריכוז"/"הפרעה"/"תזוזה") בהערות.
> 3. בונה system prompt בעברית עם המשקלים + הניתוח + ההנחיה החופשית, קורא ל-`callLovableAI` (`jsonResponse: true`), מבקש `{ placements: [{studentId, seatIndex}], reasoning: string }`.
> 4. **קריטי:** בכשל `callLovableAI` (429/402/רשת) — נופל ל-`smartAssign` הקיים מ-`seating-logic.ts` (לא בונה fallback חדש), עם `reasoning: "סודר באמצעות אלגוריתם מקומי (ללא AI) בשל עומס זמני על השירות."`.
> 5. שומרת רק אחרי אישור מורה מפורש — לא מעדכנת את הגריד ישירות.

**3.3 קומפוננטת UI — `SeatingWizardModal.tsx`:**
> modal עם 4 סליידרים (0-100%), checkbox איזון-גובה, textarea הנחיה חופשית, כפתור "הרץ סידור AI". לפני ההרצה: תקציר ניתוח-כיתה (X מתקשים, Y מצטיינים, Z קשיי-קשב). אחרי ההרצה: הצגת `reasoning` + כפתורי "אשר וסדר" / "שנה ערכים ונסה שוב". שמירת המשקלים האחרונים ב-`seating_wizard_prefs` לשימוש הבא.

---

## 📋 נדחה / עדיפות נמוכה (לא לבצע כרגע)

- **קבוצות היררכיות** (`parent_group_id`, `constraint` together/separate) — 🟡 לא אומת קוד מקור, רק רעיון מתועד. רק אם יש ביקוש בפועל.
- **מעקב מצב-רוח יומי לתלמיד** (`student_mood_log`) — פיצ'ר ייחודי ולא דחוף, מתאים לרוח הפדגוגית אך לא בעדיפות כרגע.
- **החלקת אחוז נוכחות** — רק אם קיים שדה מצטבר (לא רק לוג גולמי); לא אומת שרלוונטי.
- **פרופיל-סגנון מקומי fallback** — לא נדרש; `recomputeStyleProfile` הקיים כבר לא תלוי ב-AI.

---

## 📨 הודעות מוכנות לשליחה חוזרת ל-Lovable (ברגע שיש קרדיטים)
מ-`MERGE_MEMORY.md`, לא קשור לשכבות הבנייה למעלה אך חסום באותה סיבה:

1. **חיבור Resend לתזכורות** — לוגיקת cron/דדופ/digest קיימת ב-`reminder-alerts.server.ts`, אבל `sendReminderDigestEmail` עדיין `console.log` בלבד. הודעה מלאה מוכנה (חבילת resend, `env.RESEND_API_KEY` כ-secret בלבד — לעולם לא hardcoded, HTML בעברית RTL, טיפול שגיאות).
2. **תיקון ערכת נושא "classalign" חסרה ב-CSS** — `use-theme.tsx` מגדיר 6 ערכות אך `styles.css` מכיל בלוק `[data-theme="..."]` רק ל-5. הודעה מוכנה: בלוק `[data-theme="classalign"]` בפלטת oklch טורקיז+כתום/צהוב.

---

## הערות טכניות קבועות
- **Lovable project_id:** `2734475a-1431-4ef2-8175-67b8af357276` (repo: `cuddle-spark-nexus`, "Harmony Hub").
- **Base44 appId האמיתי:** `69efc0a68bae1b1d07582eda` (לא slug כמו `classmanager-scoll-nm`).
- כל endpoint AI חדש מכאן והלאה צריך fallback ייעודי מהיום הראשון, לא retry גנרי.
